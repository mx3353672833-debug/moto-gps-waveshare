# 待办

状态约定：`[ ]` 未开始，`[x]` 已完成。每项以 `(A)`、`(B)` 或 `(C)` 标明可移植等级；定义见 [可移植性契约](docs/PORTABILITY.md)。当前优先完成 Waveshare 1.75C + 原生 iOS App 样机；外壳设计和佳明卡口已按用户要求暂停，未经明确恢复指令不得推进、改图或打样。

## 当前冲刺：微雪实机 + iPhone

- [x] **(A)** 将 Web、Presenter 和 ESP32 唯一 framebuffer 迁移到 466 × 466 RGB565
- [x] **(C)** 增加无需定位/Key 的 `?demo=1` 四页圆屏模拟入口并完成 iPhone 尺寸视觉检查
- [x] **(A/B)** 冻结版本化 BLE GATT UUID、分片、CRC、心跳、显示快照和触摸命令
- [x] **(B)** 完成微雪官方 BSP 的 CO5300、CST9217、LVGL 和 BLE 源码接入
- [x] **(B)** 安装 ESP-IDF 5.5.5，完成微雪 `esp32s3` 真实目标构建，并在备份原厂 Flash、确认校验和与取得授权后完成首次烧录
- [x] **(B)** 完成 iOS Core Location、按手机当前位置优先的 POI 搜索、高德真实路线和 BLE 状态恢复适配；正常/生产导航只使用真实数据
- [x] **(B)** 在当前 App 测试版增加明确标注的内置导航演示适配器；在线时逐次通过现有网关实时请求“D 栋 → 浪潮总部”高德路线，网络失败时回退到同地点真实 OSM 路线，二者仍经过共享 `NavApp → NavCore → BLE` 链且不固化高德响应
- [x] **(A/B)** 修复 BLE 恢复连接时握手写入可能永久排队、旧会话回调干扰新连接以及失败后退避过早清零的问题
- [x] **(A/B)** 增加 iOS Launch Screen、ESP32 上电 1.25 秒 `MOTO GPS` Logo 淡入淡出、页面指示点 5 秒自动隐藏，以及导航页长按开关本机演示路线
- [x] **(A)** 将导航页主信息收敛为标准动作图标和大号距离数字 + 单位，不显示说明性距离标题；没有真实路线且未开启演示时隐藏动作、距离、路线进度和限速
- [x] **(A/B)** 建立“山东省大数据产业基地 D 栋附近 → 浪潮集团（总部）附近”的 canonical OSM 演示：35 个路线点 / 约 1.49 km，59 个周边道路点 / 8 个独立 span；白线与灰线均记录 `© OpenStreetMap contributors` / ODbL 署名与坐标转换边界，两个未测绘入户缺口只称“附近”
- [x] **(A)** 增加圆屏完整连接生命周期：未连接慢速扫描、握手快速扫描、联网成功绿勾约 920 ms、准备出发轻呼吸、规划中移动白点，以及有效路线后的遮罩淡出
- [x] **(A)** 状态切换采用 140 ms 淡出 + 220 ms 淡入；减少动态效果时即时切换并暂停 40 ms 动画时钟
- [x] **(A/B)** 用 Web `deviceState=offline|connecting|success|ready|planning` 逐状态完成视觉复核；共享原生测试 8/8、Swift 检查 10/10、Web 与 ESP32-S3 构建均通过
- [x] **(A/B)** 将 App 演示预览提升为 25 Hz 连续插值、BLE 导航快照按协议上限合并为 5 Hz，路线几何按签名去重，并以 15 ms 间隔发送物理分片
- [x] **(A/B)** 将 ESP32 BLE 接收与 LVGL 绘制解耦：接收线程只提交最新状态，独立低优先级任务合并导航、地图、IMU 与媒体刷新；主机测试 8/8、ESP-IDF 构建和真机连续地图传输通过
- [x] **(B)** 生成并接入完整济南离线 SQLite 矢量包：47,468 条道路、26,702 个建筑；iPhone 只向圆屏发送当前位置附近的有限窗口
- [x] **(B)** 修复 ESP32 恢复配对时 ENC_CHANGE/SUBSCRIBE 早于 CONNECT 导致握手状态被清空的问题；当前 iPhone 连续 3 次 App 冷启动握手均一次成功
- [x] **(B)** 实现 PWR 按住 3 秒软件关机，并配置、读回校验 AXP2101 按住 4 秒硬件关机兜底
- [x] **(B)** 用同一组协议 fixtures 在 C++ 与 Swift 两端通过编码/解码测试
- [x] **(B)** 备份原厂 32 MB Flash、生成并报告 SHA-256，取得明确刷写确认后首次烧录微雪板并完成启动验证
- [ ] **(B)** 长时间真机验证正常锁屏、切后台、修复后的断线重连、偏航重算和路况刷新
- [ ] **(B)** 在实体 CO5300 + iPhone 上逐项验收离线、握手、成功、准备出发、规划中和有路线六段切换，并复核反复重连、减少动态效果和无路线不显示旧导航数据
- [ ] **(B)** 评估 BLE HID 三个媒体键；“喜欢”和跨播放器曲目信息继续保持可选能力
- [x] **(B)** iOS 以公开 `systemMusicPlayer` 接通 Apple Music 上一首、播放/暂停、下一首与 MediaState 回传；无公共接口的“喜欢”保持隐藏并返回 Unsupported

## 基础契约与工程

- [x] **(A)** 建立 A/B/C 可迁移等级和修改前告知规则
- [x] **(A)** 冻结 466 × 466、RGB565 圆屏基准
- [x] **(B)** 建立共享、Web、ESP32、后端和测试目录骨架
- [x] **(B)** 固定工具链、LVGL 版本和可重复构建方式
- [x] **(A)** 让同一份 LVGL 产品画面同时通过 WebAssembly 与 ESP32 构建
- [x] **(A)** 定义并测试版本化 `RouteBundle` 协议
- [x] **(B)** 建立自有后端与高德 Provider，确保密钥不进入网页或固件
- [x] **(A)** 建立 WGS84/GCJ-02 显式坐标边界和转换测试
- [x] **(A)** 建立 Web/iOS 共用 `NavApp → NavCore`、Web/ESP32 共用 `NavPresenter → LVGL` 的分层控制链
- [x] **(A)** 实现共享路线匹配、路线推进、转向切换和到达判断
- [x] **(A)** 实现偏航确认、重算去重、旧响应丢弃和断网恢复状态机

## 已完成的开发工具：圆屏界面与网页回放

- [x] **(A)** 冻结参考视频对应的页面信息层级、车辆锚点、路线粗细和关键验收帧
- [x] **(A)** 扩展共享展示状态：页面、速度、航向、有效性、限速和音乐能力位
- [x] **(A)** 实现局部路线截取、车头朝上投影、圆形裁剪、重采样和航向平滑
- [x] **(A)** 实现固定朝上的车辆箭头、灰色周边道路、白色选中路线、动作图标和大号距离数字 + 单位；进度与限速按有效数据条件显示
- [x] **(A)** 将拥堵、偏航、重算、断网和到达状态接入新导航页
- [x] **(A)** 建立导航、马表、指南针、可选音乐四页的共享 LVGL 页面状态机
- [x] **(A)** 实现横向触摸分页、方向锁、防抖、大热区和减少动态效果模式
- [x] **(B)** 接通浏览器鼠标/触摸输入契约，并保留实体触摸控制器适配边界
- [x] **(A)** 实现独立简洁马表页：当前速度、`km/h` 和圆周刻度
- [x] **(A)** 实现独立指南针页：度数、八方位、旋转刻度和无效航向降级
- [x] **(A)** 实现可关闭的音乐控制页及播放/暂停、上一首、下一首、喜欢事件契约
- [x] **(C)** 在网页中模拟音乐状态；明确“喜欢”和曲目信息不代表 iPhone 实机能力
- [x] **(C)** 将固定路线升级为带时间、位置、速度、航向和精度的可重复导航轨迹
- [x] **(C)** 扩展网页调试台：页面、播放/暂停/倍速、进度、低速航向、偏航、断网和路况注入
- [x] **(C)** 完成桌面与手机宽度下的圆屏、触摸和控制台视觉检查
- [ ] **(C)** 完成长时间回放稳定性检查
- [x] **(A)** 建立车头朝上投影和 359°→0° 航向边界自动测试
- [ ] **(A)** 在实体 CO5300 截图基线上冻结 RGB565 golden frame 与页面状态哈希
- [ ] **(A)** 实机验证 30 fps、峰值内存和字体缺字；ESP32 目标构建一致性及 Flash 分区预算已通过

详细范围、迁移结论和退出条件见 [圆屏产品界面与导航模拟执行方案](docs/ROUND_UI_EXECUTION_PLAN.md)。

## 已完成的历史验证线：手机网页版前台导航

- [x] **(C)** 将 `maler.top/moto-gps/` 主入口改为定位 → 搜索 → 路线确认 → 导航中的手机流程
- [x] **(B)** 将浏览器 `watchPosition`、GPS speed/heading 和 iOS 方向权限收敛为共享 `GnssFix`
- [x] **(B)** 建立真实 RouteBundle/GNSS 的 WASM 输入桥与共享核心命令回调
- [x] **(B)** 接通高德 POI 2.0，返回 WGS84 导航入口并防止 GCJ-02 二次偏移
- [x] **(B)** 接通高德普通驾车 Route v2、目的地 POI ID、`cost/polyline/navi/tmcs`
- [x] **(A/B)** 用共享核心连续偏航确认，网页执行从当前位置重算
- [x] **(A/B)** 用共享核心每 60 秒调度路况刷新，网页通过 fresh route 更新 `tmcs` 与 ETA
- [x] **(C)** 接入 Screen Wake Lock、页面可见性恢复校准和前台使用警告
- [x] **(B)** 在 maler 部署 Node 网关、systemd、Nginx 同域反代、请求限流和禁用模式
- [x] **(B)** 在服务器配置高德“Web 服务 API”Key；线上健康检查已返回真实 Provider 就绪
- [ ] **(B)** 用真实 iPhone 和真实道路验证 POI、路线、偏航重算、60 秒路况刷新及长时间亮屏运行
- [ ] **(B)** 向高德确认个人验证之外的车载投射、缓存和硬件终端书面许可

详细状态和不可绕过的 iPhone 前后台边界见 [手机网页版真实导航执行方案](docs/MOBILE_WEB_NAVIGATION_PLAN.md)。

## 后续：真实导航可靠性与第二 Provider

- [x] **(B)** 接通高德 POI 搜索、普通驾车规划、路线重算和实时路况数据的软件链路
- [ ] **(B)** 评估百度作为第二 Provider；不得复制共享导航核心或圆屏 UI
- [ ] **(A/B)** 在 iPhone 权威导航端实现“有明确收益才换路”的策略
- [ ] **(A/B)** 扩展 BLE 局部路线窗口的上限、分块和低内存降级测试
- [ ] **(A)** 扩展中文道路名字库并验证 Flash、RAM 与缺字回退
- [ ] **(A)** 用进度窗口和预计算坐标优化长路线匹配性能
- [ ] **(B)** 录制真实 iPhone Core Location 日志并验证城市、高架、隧道、山路和平行道路
- [ ] **(B)** 产品化前取得高德对硬件终端、路线缓存、路况数据和商业使用的书面授权结论
- [ ] **(B)** 正式在线周边道路层开通高德“交通态势查询”高级 API，验证 `extensions=all` 的 `roads[].polyline` 覆盖率，并取得车载/智能硬件投射的明确书面授权

## 后置/暂停：自研一体主板、媒体控制与 V3 外壳

> 外壳设计与佳明卡口当前明确暂停；以下已有文件只作历史参考，未收到用户恢复指令前不继续建模、出图、采购或打样。

- [x] **(B)** 曾建立自研一体 PCB + 定制外壳 Rev A 工程；当前改用微雪成品板完成软件样机，该工程只作历史参考
- [x] **(B)** 冻结V3外观：连续铝前框、四颗对角功能螺钉、塑料后壳和隐藏快拆
- [x] **(A)** 将共享圆屏 framebuffer 迁移到目标 AMOLED 的 466 × 466，并同步 Web/ESP32
- [ ] **(B)** 取得并冻结固定料号的 1.75 英寸 466 × 466 屏幕总成、31 Pin FPC 图纸和至少 2 块实物
- [ ] **(B)** 冻结带保护/NTC 的电池实体、出线方向和续航目标
- [ ] **(B)** 若以后恢复完全独立导航版本，再评估 LC76G 与 GNSS 天线；当前 iPhone 样机不采购、不安装
- [ ] **(B)** 制作并验证 BQ25628E + TPS63070 + TUSB320LAI 电源测试券；AXP2101 不进入主板
- [x] **(B)** 完成 Rev A 六分册原理图、BOM 字段/料号完整性检查和 KiCad 10.0.6 ERC（R4：0 违规）
- [x] **(B)** 完成 Ø52 mm 四层圆板 R4 布局布线及全轨 DRC（1555 段走线、189 过孔、0 DRC、0 未连接、0 一致性错误）
- [x] **(B)** 输出 R4 审阅用 Gerber、钻孔、57 行分组 BOM、93 点 CPL、装配说明、预览和 SHA-256 清单，并完成 BOM/CPL 一致性检查
- [ ] **(B)** 若恢复自研 PCB，重新按无 GNSS 的 iPhone 外设架构审查器件与成本，再决定 1–2 块工程样板下单方式
- [ ] **(B)** 若恢复自研 PCB，按电源 → USB → ESP32 → 屏幕/触摸 → IMU/罗盘 → BLE 顺序联调裸板
- [ ] **(B)** 若以后要求停车真北方向，再建立 QMI8658 + LIS2MDL 的低速航向融合适配器
- [ ] **(B)** 当前样机验证 iPhone BLE 在息屏、弱信号和断线后的长期稳定性；不依赖手机热点维持终端连接
- [ ] **(B)** 评估并验证 BLE HID / iPhone AMS 对网易云等第三方当前播放器的播放/暂停、上一首和下一首控制；不得把 App 内 `MPRemoteCommandCenter` 当成全局命令发送器
- [ ] **(B)** 依据 AMS 当前播放器支持列表显示 `LikeTrack`；仅显示“已发送”，不承诺收藏状态或跨应用执行结果
- [ ] **(B)** 完成实体板 RAM、Flash、帧率、功耗、温度、流量和持续运行测试
- [x] **(B)** 完成 V3 Rev A0 屏幕、电池、天线和 PCB 占位的参数化外壳 CAD、STEP/STL、二维总装图与干涉检查
- [ ] **(B)** 打印全塑料 EVT 壳，装车验证 RF、指南针、触摸、散热、雨淋、振动和卡口
- [ ] **(B)** 根据 Rev A/EVT 实测完成 Rev B 主板和外壳
- [ ] **(B)** Rev B 通过后加工 CNC 铝前框 + 塑料 RF 后盖；测试前不宣称 IP67

详细硬件边界、器件选择、制造交付物和退出条件见 [自研一体硬件与外壳执行方案](docs/CUSTOM_HARDWARE_PLAN.md)。
