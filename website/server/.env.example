CONTACT_PORT=8788
CONTACT_ALLOWED_ORIGIN=https://maler.top
# Enable only behind a local proxy that overwrites X-Real-IP.
CONTACT_TRUST_PROXY=true
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM_EMAIL=
